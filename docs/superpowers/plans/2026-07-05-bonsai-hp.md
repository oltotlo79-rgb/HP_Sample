# 盆栽のあるくらし(盆栽HP)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 盆栽の魅力を発信する 5 ページ構成のビジュアル没入型静的サイトを素の HTML/CSS/JS で構築し、BON-LOG(https://www.bon-log.com/)を告知、Netlify 公開可能な状態にする。あわせて codex 向け画像生成指示書を作成する。

**Architecture:** ビルド工程なしの静的サイト。共通デザインシステムを `css/style.css` に、演出(reveal / パララックス / ヘッダー状態 / タブ / モバイルナビ)を `js/main.js` に集約し、各 HTML は共通ヘッダー・フッター・CTA バンドを持つ。画像は codex が後から `assets/images/` に生成配置する前提で、`<img width/height>` + プレースホルダー背景色でレイアウトを先に完成させる。

**Tech Stack:** HTML5 / CSS3(カスタムプロパティ)/ Vanilla JS(IntersectionObserver, rAF)/ Google Fonts(Shippori Mincho, Zen Kaku Gothic New)/ Netlify

**Spec:** `docs/superpowers/specs/2026-07-05-bonsai-hp-design.md`

---

## File Structure

```
HP_try/
  index.html          … トップ(ヒーロー/導入/魅力3章/下層誘導/BON-LOG告知)
  guide.html          … 盆栽入門(始め方3ステップ/道具/最初の一鉢)
  species.html        … 樹種図鑑(分類+6樹種)
  care.html           … 手入れ(基本3作業+四季カレンダータブ)
  culture.html        … 歴史・文化(年表/侘び寂び/海外人気)
  404.html            … Not Found ページ
  css/style.css       … デザインシステム+全ページのスタイル(単一ファイル)
  js/main.js          … 全インタラクション(単一ファイル)
  assets/favicon.svg  … ファビコン
  assets/images/      … codex 生成画像の配置先(実装時は空)
  netlify.toml        … Netlify 設定
  .gitignore
  docs/plans/image-generation-brief.md … codex 向け画像生成指示書
```

デザイントークン(スペック確定値):

| トークン | 値 |
|---|---|
| `--c-base` 生成り | `#F6F3EC` |
| `--c-pine` 深松葉色 | `#2E4B3C` |
| `--c-brown` 焦茶 | `#4A3728` |
| `--c-night` ダーク背景 | `#161B17` |
| `--c-moss` 苔色(添え色限定) | `#8A9A5B` |
| `--c-kinoko` BON-LOG鳥の子色 | `#F0ECE4` |
| `--c-yozora` BON-LOG夜空色 | `#1A1A2E` |
| 見出しフォント | 'Shippori Mincho', serif |
| 本文フォント | 'Zen Kaku Gothic New', sans-serif |

---

### Task 1: プロジェクト基盤(ディレクトリ・netlify.toml・.gitignore)

**Files:**
- Create: `netlify.toml`
- Create: `.gitignore`
- Create: `assets/images/.gitkeep`

- [ ] **Step 1: .gitignore を作成**

```gitignore
.DS_Store
Thumbs.db
node_modules/
*.log
```

- [ ] **Step 2: netlify.toml を作成**

```toml
[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
```

- [ ] **Step 3: assets/images/.gitkeep を作成し、ディレクトリ構造を確定**

空ファイル `assets/images/.gitkeep` を作成(codex 画像の配置先を確保)。

- [ ] **Step 4: Commit**

```bash
git add .gitignore netlify.toml assets
git commit -m "chore: プロジェクト基盤(netlify設定・ディレクトリ)を追加"
```

---

### Task 2: 画像生成指示書(codex 向け)

**Files:**
- Create: `docs/plans/image-generation-brief.md`

このタスクを先行させ、実装と並行して codex に画像生成を依頼できるようにする。

- [ ] **Step 1: 指示書の骨格と共通スタイルガイドを書く**

指示書は次の構成で書く(すべて日本語の説明+英語プロンプト):

1. **概要**: 目的(盆栽HP用の写真調画像)、保存先(`assets/images/`)、フォーマット(WebP・品質80)、命名規則。
2. **全画像共通スタイルガイド(必ず各プロンプトに含める)**:
   - Photorealistic, professional photography(DSLR, 50mm/85mm 相当)
   - 自然光(朝の斜光 or 曇天のやわらかい光)、浅い被写界深度
   - 和の背景(縁側・木のテーブル・和紙・苔・砂利・床の間)。ごちゃつく背景禁止
   - カラーグレーディング: 生成り `#F6F3EC`・深緑 `#2E4B3C`・焦茶 `#4A3728` に調和する落ち着いた低〜中彩度。過飽和禁止
   - 共通英語サフィックス例: `photorealistic, professional photography, soft natural morning light, shallow depth of field, muted earthy color palette (cream #F6F3EC, deep pine green #2E4B3C, dark brown #4A3728), Japanese aesthetic, high detail, 8k`
   - **共通ネガティブ指定**: `oversaturated colors, HDR look, plastic-looking leaves, deformed branches, warped pot, text, watermark, logo, people's faces, illustration, painting, CGI look`
3. **画像一覧表**(下の Step 2 の表をそのまま採用)
4. **画像ごとの個別指示**(1画像 = 1見出し。ファイル名/配置先/表示箇所/アスペクト比/出力解像度/被写体の詳細/完全な英語プロンプト/個別ネガティブ)
5. **BON-LOG 用画像についての注記**: `bonlog-app.webp` は**実物スクリーンショットの使用を推奨**(本物感のため)。生成する場合の代替プロンプト(スマートフォンのモックアップにアプリ風UI)も併記するが、UI 文字の破綻が起きやすいため screenshot 優先と明記。

- [ ] **Step 2: 画像一覧表を完成させる**

全 29 枚。表の列: ID / ファイル名 / 使用ページ・セクション / アスペクト比 / 出力解像度 / 被写体。

| ID | ファイル名 | 使用箇所 | 比率 | 解像度 | 被写体 |
|---|---|---|---|---|---|
| 01 | `hero-top.webp` | index ヒーロー(全画面背景) | 16:9 | 2400×1350 | 床の間に置かれた樹齢を感じる黒松の盆栽、朝の斜光 |
| 02 | `intro-hands.webp` | index 導入(左寄せ) | 3:2 | 1600×1067 | 職人の手が鋏で枝を整える手元のクローズアップ |
| 03 | `chapter-time.webp` | index 第一章「時間を育てる」(全画面・ダーク) | 16:9 | 2400×1350 | 暗背景に浮かぶ古木盆栽のシルエット、ドラマチックな一筋の光 |
| 04 | `chapter-nature.webp` | index 第二章「自然を写す」(全画面) | 16:9 | 2400×1350 | 苔と紅葉した葉のマクロ、雨上がりの水滴 |
| 05 | `chapter-form.webp` | index 第三章「かたちを見立てる」(全画面) | 16:9 | 2400×1350 | 文人木の流れるような幹の曲線、余白を活かした構図 |
| 06 | `nav-guide.webp` | index 下層誘導カード | 4:3 | 1200×900 | 作業台の上の小さな初心者向け盆栽と道具 |
| 07 | `nav-species.webp` | index 下層誘導カード | 4:3 | 1200×900 | 棚に並ぶ異なる樹種の盆栽たち |
| 08 | `nav-care.webp` | index 下層誘導カード | 4:3 | 1200×900 | 銅のじょうろで水やりする瞬間 |
| 09 | `nav-culture.webp` | index 下層誘導カード | 4:3 | 1200×900 | 掛け軸のある床の間に飾られた盆栽 |
| 10 | `bonlog-app.webp` | index BON-LOG告知 | 3:4 | 900×1200 | ※実物スクショ推奨。代替: 盆栽の横に置かれたスマホに記録アプリ画面 |
| 11 | `hero-guide.webp` | guide ヒーロー | 16:9 | 2400×1350 | 明るい縁側で初めての一鉢に向き合う手元 |
| 12 | `step-choose.webp` | guide ステップ1 | 4:3 | 1200×900 | 園芸店で小品盆栽を選ぶ手元 |
| 13 | `step-place.webp` | guide ステップ2 | 4:3 | 1200×900 | 日当たりの良いベランダの棚に置かれた盆栽 |
| 14 | `step-water.webp` | guide ステップ3 | 4:3 | 1200×900 | 土に注がれる水、細かい水流のクローズアップ |
| 15 | `tools.webp` | guide 道具紹介 | 3:2 | 1600×1067 | 帆布の上に整然と並ぶ盆栽鋏・ピンセット・針金・回転台 |
| 16 | `first-tree.webp` | guide 最初の一鉢 | 3:2 | 1600×1067 | 丈夫な真柏の小品盆栽、シンプルな鉢 |
| 17 | `hero-species.webp` | species ヒーロー | 16:9 | 2400×1350 | 展示棚に並ぶ多様な樹種、奥行きのある構図 |
| 18 | `sp-kuromatsu.webp` | species 黒松 | 16:9 | 2000×1125 | 荒々しい幹肌と力強い枝ぶりの黒松 |
| 19 | `sp-goyomatsu.webp` | species 五葉松 | 16:9 | 2000×1125 | 短い葉が密につく気品ある五葉松 |
| 20 | `sp-shimpaku.webp` | species 真柏 | 16:9 | 2000×1125 | 白い舎利(枯れ幹)と緑のコントラストが美しい真柏 |
| 21 | `sp-momiji.webp` | species もみじ | 16:9 | 2000×1125 | 紅葉の盛りのもみじ盆栽、逆光で透ける葉 |
| 22 | `sp-keyaki.webp` | species 欅 | 16:9 | 2000×1125 | ほうき立ちの美しい欅、冬の枝ぶり(寒樹) |
| 23 | `sp-ume.webp` | species 梅 | 16:9 | 2000×1125 | 古木に白い花が咲く梅盆栽、早春の光 |
| 24 | `hero-care.webp` | care ヒーロー | 16:9 | 2400×1350 | 朝の水やり風景、水滴と光 |
| 25 | `care-pruning.webp` | care 剪定 | 3:2 | 1600×1067 | 鋏で芽を摘む精密な手元 |
| 26 | `care-repotting.webp` | care 植替え | 3:2 | 1600×1067 | 鉢から抜いた根鉢をほぐす作業 |
| 27 | `season-spring.webp` / `season-summer.webp` / `season-autumn.webp` / `season-winter.webp` | care 四季タブ(4枚) | 4:3 | 各1200×900 | 春: 芽吹き / 夏: 葉水と木漏れ日 / 秋: 紅葉と施肥 / 冬: 雪をかぶった枝(保護) |
| 28 | `hero-culture.webp` | culture ヒーロー | 16:9 | 2400×1350 | 歴史を感じる名品盆栽、美術館のような静謐な照明 |
| 29 | `culture-wabisabi.webp` | culture 侘び寂び | 3:2 | 1600×1067 | 苔むした石灯籠と盆栽、経年の美 |
| +OG | `og-image.webp`(PNG併用可) | 全ページ OGP | 1.91:1 | 1200×630 | 01 と同系統の構図に余白(テキストは入れない) |

- [ ] **Step 3: 各画像の個別指示(完全な英語プロンプト)を書き切る**

上表の全画像について、次のフォーマットで 1 枚ずつ個別セクションを書く。プロンプトは被写体の詳細+共通サフィックスを結合した完成形で記載し、コピペで生成できる状態にする。

````markdown
### 01. hero-top.webp
- 保存先: `assets/images/hero-top.webp`
- 使用箇所: `index.html` ヒーローセクション(全画面背景、`object-fit: cover`)
- アスペクト比 / 解像度: 16:9 / 2400×1350px、WebP 品質80
- 表示上の注意: 中央〜やや右に主題を置く(左側に縦書きコピーが載るため左1/3は余白気味に)
- Prompt:
  > An ancient Japanese black pine bonsai (kuromatsu) with thick, rugged bark displayed in a traditional tokonoma alcove, soft morning side-light raking across the moss-covered soil, negative space on the left third of the frame, photorealistic, professional photography, soft natural morning light, shallow depth of field, muted earthy color palette (cream #F6F3EC, deep pine green #2E4B3C, dark brown #4A3728), Japanese aesthetic, high detail, 8k
- Negative:
  > oversaturated colors, HDR look, plastic-looking leaves, deformed branches, warped pot, text, watermark, logo, people's faces, illustration, painting, CGI look
````

(全 29 枚+OG 画像について、被写体列の内容を英語に展開し同じ形式で記載する。「〜と同様」による省略は禁止。)

- [ ] **Step 4: Commit**

```bash
git add docs/plans/image-generation-brief.md
git commit -m "docs: codex向け画像生成指示書を追加"
```

---

### Task 3: CSS デザインシステム(css/style.css 前半)

**Files:**
- Create: `css/style.css`

- [ ] **Step 1: トークン・リセット・ベースタイポグラフィを書く**

```css
/* ===== Design tokens ===== */
:root {
  --c-base: #F6F3EC;
  --c-pine: #2E4B3C;
  --c-brown: #4A3728;
  --c-night: #161B17;
  --c-moss: #8A9A5B;
  --c-kinoko: #F0ECE4;
  --c-yozora: #1A1A2E;
  --c-ink: #26221C;          /* 本文文字色 */
  --c-ink-light: #F2EFE8;    /* ダーク背景上の文字色 */
  --font-serif: 'Shippori Mincho', 'Hiragino Mincho ProN', serif;
  --font-sans: 'Zen Kaku Gothic New', 'Hiragino Kaku Gothic ProN', sans-serif;
  --space-section: clamp(5rem, 12vw, 10rem);
  --width-content: 1080px;
  --width-text: 42rem;
}

/* ===== Reset / base ===== */
*, *::before, *::after { box-sizing: border-box; }
body {
  margin: 0;
  background: var(--c-base);
  color: var(--c-ink);
  font-family: var(--font-sans);
  font-size: 1rem;
  line-height: 2;
  letter-spacing: 0.06em;
  -webkit-font-smoothing: antialiased;
}
img { max-width: 100%; height: auto; display: block; background: #E5E0D5; }
h1, h2, h3 { font-family: var(--font-serif); font-weight: 500; line-height: 1.6; letter-spacing: 0.12em; }
a { color: inherit; }
:focus-visible { outline: 2px solid var(--c-moss); outline-offset: 3px; }
```

`img` の `background` が画像未生成時のプレースホルダーを兼ねる。

- [ ] **Step 2: 共通コンポーネント(ヘッダー/フッター/ボタン/CTAバンド/reveal)を書く**

実装するクラス契約(HTML 側はこの名前を使う):

- `.site-header` — 固定ヘッダー。初期は透明+白文字、`.is-scrolled` が付くと生成り背景+墨色文字に遷移
- `.site-header__nav` / `.nav-toggle`(モバイル用ボタン)/ `.site-header__links.is-open`
- `.btn` / `.btn--light`(ダーク背景用)/ `.btn--bonlog`(BON-LOG CTA、夜空色 `--c-yozora` 背景)/ `.btn--bonlog-nav`(ヘッダーナビ内の小型 BON-LOG リンク、枠線スタイル)
- `.cta-band` — 全ページ共通のフッター直前 CTA(鳥の子色背景、BON-LOG へ外部リンク)
- `.site-footer` — 夜色背景、サイト名・ページ内リンク・BON-LOG リンク・コピーライト
- `.reveal` — 初期 `opacity: 0; translateY(24px)`、`.is-visible` で 0.9s かけて表示。**`@media (prefers-reduced-motion: reduce)` では transition/transform を無効化して常時表示**
- `.v-title` — 縦書き章タイトル(`writing-mode: vertical-rl`)

JS 無効環境でもコンテンツが見えるよう、`.reveal` の初期非表示は `html.js-enabled` 配下に限定する(main.js が先頭でこのクラスを付与する):

```css
html.js-enabled .reveal { opacity: 0; transform: translateY(24px); transition: opacity .9s ease, transform .9s ease; }
html.js-enabled .reveal.is-visible { opacity: 1; transform: none; }
@media (prefers-reduced-motion: reduce) {
  .reveal { opacity: 1; transform: none; transition: none; }
  * { animation: none !important; scroll-behavior: auto !important; }
}
.v-title { writing-mode: vertical-rl; letter-spacing: 0.35em; font-family: var(--font-serif); }
```

ヘッダー・フッター・ボタン・CTA バンドも同スタイルシートに完全実装する(値はトークン参照)。

- [ ] **Step 3: セクションレイアウト(全画面章・交互配置・カードグリッド)を書く**

- `.hero` — `min-height: 100svh`。背景画像は `.hero__bg`(`position:absolute; object-fit:cover;` JS がパララックス対象とする `data-parallax` 付き)。手前に `.hero__inner`(縦書きコピー+サブコピー)
- `.chapter` — 全画面写真セクション(`min-height: 90svh`)。`.chapter--dark` は `--c-night` 背景+`--c-ink-light` 文字
- `.split` — 写真+本文の 2 カラム交互配置(`.split--reverse`)。モバイルで 1 カラム
- `.card-grid` — 下層誘導 4 枚 / 樹種などのカード。`grid-template-columns: repeat(auto-fit, minmax(240px, 1fr))`
- ブレークポイント: `max-width: 1024px` で余白縮小、`max-width: 768px` で 1 カラム+ハンバーガー表示

- [ ] **Step 4: ローカルで CSS 構文確認と Commit**

Run: `python -m http.server 8000`(HP_try 直下)→ 後続タスクの HTML から読み込んで確認するため、この時点では構文エラーがないことのみ確認(VS Code の Problems / 目視)。

```bash
git add css/style.css
git commit -m "feat: デザインシステム(トークン・共通コンポーネント・レイアウト)を追加"
```

---

### Task 4: JavaScript(js/main.js 完全実装)

**Files:**
- Create: `js/main.js`

- [ ] **Step 1: main.js を書く(以下の完全実装)**

```js
/* 盆栽のあるくらし — interactions */
(() => {
  'use strict';

  document.documentElement.classList.add('js-enabled');

  const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  /* --- Header state --- */
  const header = document.querySelector('.site-header');
  const onScroll = () => {
    if (!header) return;
    header.classList.toggle('is-scrolled', window.scrollY > 40);
  };
  document.addEventListener('scroll', onScroll, { passive: true });
  onScroll();

  /* --- Mobile nav --- */
  const toggle = document.querySelector('.nav-toggle');
  const links = document.querySelector('.site-header__links');
  if (toggle && links) {
    toggle.addEventListener('click', () => {
      const open = links.classList.toggle('is-open');
      toggle.setAttribute('aria-expanded', String(open));
    });
    links.addEventListener('click', (e) => {
      if (e.target.closest('a')) {
        links.classList.remove('is-open');
        toggle.setAttribute('aria-expanded', 'false');
      }
    });
  }

  /* --- Scroll reveal --- */
  const revealTargets = document.querySelectorAll('.reveal');
  if (revealTargets.length && 'IntersectionObserver' in window && !prefersReduced) {
    const io = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          io.unobserve(entry.target);
        }
      });
    }, { threshold: 0.18 });
    revealTargets.forEach((el) => io.observe(el));
  } else {
    revealTargets.forEach((el) => el.classList.add('is-visible'));
  }

  /* --- Hero parallax (subtle) --- */
  const parallaxEls = document.querySelectorAll('[data-parallax]');
  if (parallaxEls.length && !prefersReduced) {
    let ticking = false;
    const update = () => {
      parallaxEls.forEach((el) => {
        const rect = el.parentElement.getBoundingClientRect();
        if (rect.bottom > 0 && rect.top < window.innerHeight) {
          el.style.transform = `translateY(${rect.top * -0.15}px)`;
        }
      });
      ticking = false;
    };
    document.addEventListener('scroll', () => {
      if (!ticking) { requestAnimationFrame(update); ticking = true; }
    }, { passive: true });
    update();
  }

  /* --- Season tabs (care.html) --- */
  const tablist = document.querySelector('[role="tablist"]');
  if (tablist) {
    const tabs = Array.from(tablist.querySelectorAll('[role="tab"]'));
    const panels = tabs.map((t) => document.getElementById(t.getAttribute('aria-controls')));
    const select = (tab) => {
      tabs.forEach((t, i) => {
        const active = t === tab;
        t.setAttribute('aria-selected', String(active));
        t.tabIndex = active ? 0 : -1;
        panels[i].hidden = !active;
      });
      tab.focus();
    };
    tabs.forEach((tab) => tab.addEventListener('click', () => select(tab)));
    tablist.addEventListener('keydown', (e) => {
      const i = tabs.indexOf(document.activeElement);
      if (i < 0) return;
      if (e.key === 'ArrowRight') select(tabs[(i + 1) % tabs.length]);
      if (e.key === 'ArrowLeft') select(tabs[(i - 1 + tabs.length) % tabs.length]);
    });
  }
})();
```

- [ ] **Step 2: Commit**

```bash
git add js/main.js
git commit -m "feat: インタラクション(reveal・パララックス・ナビ・四季タブ)を実装"
```

---

### Task 5: 共通レイアウト確定 + index.html

**Files:**
- Create: `index.html`
- Create: `assets/favicon.svg`
- Modify: `css/style.css`(index 固有スタイル追記)

- [ ] **Step 1: favicon.svg を作成**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="12" fill="#2E4B3C"/>
  <text x="32" y="44" font-family="serif" font-size="34" fill="#F6F3EC" text-anchor="middle">盆</text>
</svg>
```

- [ ] **Step 2: index.html の共通部(head / ヘッダー / CTAバンド / フッター)を書く**

この構造を**全ページ共通テンプレート**とする(タイトル・description・OGP・アクティブナビのみページごとに変える):

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>盆栽のあるくらし — 小さな鉢の、大きな宇宙。</title>
  <meta name="description" content="盆栽の魅力を、はじめての人へ。始め方・樹種・手入れ・歴史をやさしく紹介し、盆栽愛好家のためのSNS「BON-LOG」をご案内します。">
  <meta property="og:type" content="website">
  <meta property="og:title" content="盆栽のあるくらし — 小さな鉢の、大きな宇宙。">
  <meta property="og:description" content="盆栽の魅力を、はじめての人へ。">
  <meta property="og:image" content="/assets/images/og-image.webp">
  <meta property="og:locale" content="ja_JP">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@400;500;600&family=Zen+Kaku+Gothic+New:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="site-header">
    <a class="site-header__logo" href="/">盆栽のあるくらし</a>
    <button class="nav-toggle" aria-expanded="false" aria-label="メニュー"><span></span><span></span></button>
    <nav class="site-header__nav" aria-label="サイト内">
      <ul class="site-header__links">
        <li><a href="/guide.html">はじめる</a></li>
        <li><a href="/species.html">樹種を知る</a></li>
        <li><a href="/care.html">育てる</a></li>
        <li><a href="/culture.html">歴史と文化</a></li>
        <li><a class="btn--bonlog-nav" href="https://www.bon-log.com/" target="_blank" rel="noopener">BON-LOG</a></li>
      </ul>
    </nav>
  </header>

  <main><!-- ページ固有コンテンツ --></main>

  <section class="cta-band">
    <p class="cta-band__copy">あなたの盆栽を、記録しよう。</p>
    <p class="cta-band__sub">盆栽愛好家のためのSNS「BON-LOG」— 成長記録・手入れカレンダー・盆栽園マップ。無料で始められます。</p>
    <a class="btn btn--bonlog" href="https://www.bon-log.com/" target="_blank" rel="noopener">BON-LOG を見てみる</a>
  </section>

  <footer class="site-footer">
    <p class="site-footer__logo">盆栽のあるくらし</p>
    <nav aria-label="フッター">
      <ul>
        <li><a href="/guide.html">盆栽入門</a></li>
        <li><a href="/species.html">樹種図鑑</a></li>
        <li><a href="/care.html">手入れ・育て方</a></li>
        <li><a href="/culture.html">歴史・文化</a></li>
        <li><a href="https://www.bon-log.com/" target="_blank" rel="noopener">BON-LOG(外部)</a></li>
      </ul>
    </nav>
    <p class="site-footer__copyright">&copy; 2026 盆栽のあるくらし</p>
  </footer>
  <script src="/js/main.js" defer></script>
</body>
</html>
```

- [ ] **Step 3: index の main コンテンツを書く**

セクション構成と実コピー(見出し・リード文はこのまま使用。本文は各リードの趣旨を 2〜4 文で肉付けする):

1. **ヒーロー** `.hero` — `hero-top.webp`(2400×1350, `fetchpriority="high"`, lazy なし)。縦書き `.v-title` で「小さな鉢の、大きな宇宙。」、サブコピー「掌にのる一鉢に、百年の時間と、四季のめぐりが宿る。」、下部にスクロール誘導(`↓ scroll`)。
2. **導入** `.split` — `intro-hands.webp`。h2「盆栽とは、生きた芸術。」本文: 鉢の上に自然の風景を写しとる日本の伝統文化であること、完成がなく育て続けるものであることを 3 文程度で。
3. **第一章** `.chapter.chapter--dark` — `chapter-time.webp`。縦書き「時間を育てる」。本文: 盆栽の時間は人の時間よりゆっくり流れる。十年で枝が決まり、五十年で幹が語りはじめる。世代を超えて受け継がれる樹もある。
4. **第二章** `.chapter` — `chapter-nature.webp`。「自然を写す」。本文: 芽吹き、深緑、紅葉、寒樹。鉢の中で四季がめぐる。自然の景色を数十センチに凝縮する「見立て」の面白さ。
5. **第三章** `.chapter` — `chapter-form.webp`。「かたちを見立てる」。本文: 幹の曲、根張り、枝ぶり。同じ樹は二つとない。育て手の美意識が樹形に表れる。
6. **下層誘導** `.card-grid` — 4 カード(nav-guide / nav-species / nav-care / nav-culture 画像、各ページタイトル+一行リード+リンク)。
7. **BON-LOG 告知(大型)** `.bonlog` — 背景 `--c-kinoko`、見出しは夜空色。h2「育てる楽しみを、記録する。」/ サブ「BON-LOG — 盆栽愛好家のためのコミュニティSNS」。`bonlog-app.webp`(900×1200)を右に、左に機能リスト:
   - 樹ごとの成長記録と写真アルバム
   - 剪定・水やり・施肥を残せる手入れカレンダー
   - 全国の盆栽園マップとレビュー
   - 農林水産省データに基づく薬剤・病害虫データベース
   - 「無料で始められます。Google アカウントでもログインできます。」+ `btn--bonlog` CTA

すべての `<img>` に `width`/`height`/`alt` を付け、ヒーロー以外は `loading="lazy"`。各セクションの見出し・本文ブロックに `.reveal` を付与。

- [ ] **Step 4: index 固有スタイルを style.css に追記**

`.hero`, `.chapter`, `.split`, `.card-grid`, `.bonlog` の詳細スタイル(Task 3 のクラス契約に沿って実装)。

- [ ] **Step 5: 表示確認**

Run: `python -m http.server 8000` → http://localhost:8000/
Expected: 全セクションがプレースホルダー背景色で崩れなく表示。スクロールで reveal / ヘッダー変化 / パララックスが動く。コンソールエラーなし。モバイル幅(375px)でハンバーガーメニューが機能。

- [ ] **Step 6: Commit**

```bash
git add index.html assets/favicon.svg css/style.css
git commit -m "feat: トップページ(ヒーロー・魅力3章・BON-LOG告知)を実装"
```

---

### Task 6: guide.html(盆栽入門)

**Files:**
- Create: `guide.html`(Task 5 の共通テンプレートを流用、title/description/OGP を差し替え)
- Modify: `css/style.css`(必要ならステップ番号などの小スタイル追記)

- [ ] **Step 1: ページを書く**

- title: 「盆栽入門 — 盆栽のあるくらし」/ description: 「初めての盆栽の始め方を3ステップで。必要な道具と、最初の一鉢の選び方。」
- **ヒーロー(小型: `min-height: 60svh`)** — `hero-guide.webp`。h1「はじめての盆栽」リード「難しく考えなくて、いい。一鉢から始まる暮らし。」
- **3ステップ**(`.split` 交互配置、ステップ番号は明朝の漢数字「一・二・三」を大きく):
  - 一. 一鉢を選ぶ — `step-choose.webp`。本文: 最初は樹の完成形より「好きだと思えるか」で選ぶ。小品盆栽なら数千円から。
  - 二. 置き場所を決める — `step-place.webp`。本文: 盆栽は屋外の植物。日当たりと風通しの良いベランダや庭先が基本。室内観賞は数日まで。
  - 三. 水やりを覚える — `step-water.webp`。本文: 「土の表面が乾いたら、鉢底から流れるまでたっぷり」。毎日決まった量ではなく、樹と土を見て判断するのが上達の第一歩。
- **道具紹介** `.split` — `tools.webp`。h2「そろえたい道具」: 盆栽鋏・ピンセット・じょうろ(はす口)・針金。最初は鋏とじょうろだけでも十分、と添える。
- **最初の一鉢** `.split` — `first-tree.webp`。h2「最初の一鉢におすすめの樹」: 真柏(丈夫で枯れにくい)・もみじ(季節の変化が楽しい)・黒松(盆栽らしさの王道)。それぞれ一行の理由付き。樹種図鑑(species.html)への内部リンク。

- [ ] **Step 2: 表示確認(http://localhost:8000/guide.html、デスクトップ/375px)**

- [ ] **Step 3: Commit**

```bash
git add guide.html css/style.css
git commit -m "feat: 盆栽入門ページを実装"
```

---

### Task 7: species.html(樹種図鑑)

**Files:**
- Create: `species.html`
- Modify: `css/style.css`(樹種セクション用スタイル)

- [ ] **Step 1: ページを書く**

- title: 「樹種図鑑 — 盆栽のあるくらし」/ description: 「黒松・五葉松・真柏・もみじ・欅・梅。盆栽の代表的な樹種の魅力を紹介。」
- **ヒーロー** — `hero-species.webp`。h1「樹種図鑑」リード「松の風格、雑木の四季、花の便り。樹が変われば、楽しみも変わる。」
- **分類の説明**(テキストセクション): 松柏盆栽(常緑針葉樹。盆栽の王道)/ 雑木盆栽(落葉樹。四季の変化)/ 花もの・実もの(花や実を楽しむ)を短く定義。
- **樹種セクション × 6**(全画面幅写真 16:9 +重ねテキスト or 直下テキストの繰り返し。`.species-item`):
  1. 黒松(くろまつ)— 松柏|「男松」と呼ばれる力強さ。荒々しい幹肌と濃緑の葉。
  2. 五葉松(ごようまつ)— 松柏|短い葉が五本ずつ束になる気品。祝いの席にも好まれる。
  3. 真柏(しんぱく)— 松柏|白い舎利と生きた水吸いのコントラスト。丈夫で初心者にも。
  4. もみじ — 雑木|春の芽出し、夏の緑陰、秋の紅葉。一年中表情が変わる。
  5. 欅(けやき)— 雑木|ほうき立ちの端正な樹形。葉を落とした冬の枝ぶり(寒樹)こそ見どころ。
  6. 梅 — 花もの|古木に咲く早春の花と香り。「花は盆栽の便り」。
  各樹種に「分類タグ」「難易度(やさしい/ふつう/挑戦)」を小さく添える。
- 末尾に guide.html への誘導(「最初の一鉢を選ぶなら → 盆栽入門へ」)。

- [ ] **Step 2: 表示確認(http://localhost:8000/species.html)**

- [ ] **Step 3: Commit**

```bash
git add species.html css/style.css
git commit -m "feat: 樹種図鑑ページを実装"
```

---

### Task 8: care.html(手入れ・育て方+四季タブ)

**Files:**
- Create: `care.html`
- Modify: `css/style.css`(タブ UI スタイル)

- [ ] **Step 1: ページを書く**

- title: 「手入れ・育て方 — 盆栽のあるくらし」/ description: 「水やり・剪定・植替えの基本と、春夏秋冬の作業カレンダー。」
- **ヒーロー** — `hero-care.webp`。h1「育てる、という愉しみ」リード「手をかけた分だけ、樹は応えてくれる。」
- **基本の三作業**(`.split` 交互配置):
  - 水やり — `step-water.webp` を再利用(guide と共通でよい)。「乾いたらたっぷり」の原則、季節による頻度目安(春秋1日1回/夏1日2回/冬2〜3日に1回)。
  - 剪定 — `care-pruning.webp`。不要な枝を抜いて風と光を通す。切るのは「迷ったら切らない」から。
  - 植替え — `care-repotting.webp`。2〜3年に一度、春の彼岸ごろ。根を整理して新しい土へ。
- **四季の作業カレンダー(タブ UI)** — `role="tablist"` / `role="tab"` / `role="tabpanel"`(main.js の実装と対応)。各パネル: 季節画像(`season-*.webp`)+作業リスト:
  - 春: 植替え・芽摘み・芽出し肥。一年でいちばん忙しく、いちばん楽しい季節。
  - 夏: 水切れ注意(朝夕2回)・葉水・半日陰への移動。
  - 秋: 施肥・針金かけ・紅葉の観賞。来年の樹形をつくる季節。
  - 冬: 寒風と凍結からの保護(棚下・ムロ)・寒樹の観賞・古葉取り。
- 末尾に BON-LOG への文脈的誘導: 「日々の手入れは、記録すると上達が早い。BON-LOG の手入れカレンダーなら剪定・水やり・施肥をワンタップで残せます。」+リンク。

- [ ] **Step 2: タブの動作確認**

http://localhost:8000/care.html で、クリック切替・矢印キー切替・初期状態(春が選択)を確認。

- [ ] **Step 3: Commit**

```bash
git add care.html css/style.css
git commit -m "feat: 手入れページ(基本三作業・四季カレンダータブ)を実装"
```

---

### Task 9: culture.html(歴史・文化)

**Files:**
- Create: `culture.html`
- Modify: `css/style.css`(年表スタイル)

- [ ] **Step 1: ページを書く**

- title: 「歴史・文化 — 盆栽のあるくらし」/ description: 「唐の盆景から世界の BONSAI へ。千年を超える歴史と侘び寂びの美学。」
- **ヒーロー** — `hero-culture.webp`。h1「千年を、鉢の上に」リード「盆栽は、時代を超えて受け継がれてきた生きた文化財。」
- **年表**(縦のタイムライン `.timeline`):
  - 平安〜鎌倉|中国・唐の「盆景」が伝来。絵巻物(春日権現験記)に鉢植えの樹が描かれる。
  - 室町〜江戸|武家・庶民に広がる。園芸文化が爛熟し、江戸は世界有数の園芸都市に。
  - 明治|「盆栽」の呼び名が定着。政財界人の教養として愛好される。
  - 大正14年(1925)|関東大震災を機に、東京の盆栽業者が大宮に移住し「大宮盆栽村」が誕生。
  - 1970|大阪万博で海外に広く紹介され、"BONSAI" が世界の言葉に。
  - 1989|第1回世界盆栽大会がさいたま(大宮)で開催。現在は世界中に愛好家コミュニティ。
- **侘び寂びの美学** `.split` — `culture-wabisabi.webp`。h2「侘び寂びと盆栽」: 完璧ではなく、経年の味わいに美を見る感性。枯れ枝(舎利)や苔むした鉢肌さえ見どころになる。余白と非対称の美。
- **世界の BONSAI**(テキスト+ダークセクション): 欧米・アジアに愛好団体、各国で展示会。日本語の "bonsai" がそのまま国際語になっている。
- 末尾に「あなたも、この千年の続きを育ててみませんか。」→ guide.html への誘導。

- [ ] **Step 2: 表示確認(http://localhost:8000/culture.html)**

- [ ] **Step 3: Commit**

```bash
git add culture.html css/style.css
git commit -m "feat: 歴史・文化ページを実装"
```

---

### Task 10: 404 ページ

**Files:**
- Create: `404.html`

- [ ] **Step 1: 404.html を書く**

共通ヘッダー/フッターを流用したシンプルなページ。h1「404 — 見つかりませんでした」、コピー「お探しのページは、植え替えられたようです。」、トップへ戻るボタン。Netlify は `404.html` を自動で使用する。

- [ ] **Step 2: Commit**

```bash
git add 404.html
git commit -m "feat: 404ページを追加"
```

---

### Task 11: 総合検証

**Files:** なし(検証のみ。修正が出たら該当ファイルを直してコミット)

- [ ] **Step 1: 全ページ表示確認**

Run: `python -m http.server 8000` → 全 5 ページ+404 をデスクトップ幅/375px で確認。
Expected: レイアウト崩れなし、コンソールエラーなし、ナビの現在ページ表示が正しい。

- [ ] **Step 2: リンクチェック**

全ページの内部リンク(ナビ・フッター・本文内)と BON-LOG 外部リンク(`target="_blank" rel="noopener"`)を辿って 404 がないこと。

- [ ] **Step 3: prefers-reduced-motion 確認**

DevTools → Rendering → Emulate CSS prefers-reduced-motion: reduce で、コンテンツが全て初期表示され、パララックスが無効なこと。

- [ ] **Step 4: JS 無効確認**

DevTools で JS を無効化 → 全コンテンツが閲覧可能なこと(Task 3 で `.reveal` の初期非表示を `html.js-enabled` 配下に限定済みのため、JS 無効時は全要素が表示されるはず)。

- [ ] **Step 5: Lighthouse 実行**

Chrome DevTools → Lighthouse(モバイル)を index.html で実行。
Expected: Accessibility / Best Practices / SEO 90+。Performance は画像未生成のため参考値(画像配置後に再計測)。

- [ ] **Step 6: 最終 Commit**

```bash
git add -A
git commit -m "fix: 総合検証での修正"
```

---

## 実装後の残作業(ユーザー側)

1. `docs/plans/image-generation-brief.md` を codex に渡して画像を生成、`assets/images/` に配置。
2. BON-LOG の実物スクリーンショットを撮影して `bonlog-app.webp` として配置(推奨)。
3. 画像配置後に Lighthouse Performance を再計測。
4. Netlify にデプロイ(git 連携 or フォルダドラッグ&ドロップ)。
